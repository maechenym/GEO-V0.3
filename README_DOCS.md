# 文档系统使用说明

## 📁 文档存放位置

**所有文档请放在 `content/docs/` 目录下**

### 文件结构示例

```
content/docs/
├── index.mdx              # 文档首页 (/docs)
├── quickstart.mdx         # 快速开始 (/docs/quickstart)
├── metrics/
│   ├── index.mdx          # 指标概览 (/docs/metrics)
│   ├── visibility.mdx     # 可见度指标 (/docs/metrics/visibility)
│   ├── influence.mdx      # 影响力指标 (/docs/metrics/influence)
│   └── sentiment.mdx      # 情绪分析 (/docs/metrics/sentiment)
├── setup/
│   ├── brand.mdx          # 品牌设置 (/docs/setup/brand)
│   ├── products.mdx       # 产品设置 (/docs/setup/products)
│   └── competitors.mdx   # 竞争对手设置 (/docs/setup/competitors)
└── ...
```

## ✍️ 文档格式

### 1. 文件扩展名
- 使用 `.mdx` 或 `.md` 扩展名
- 推荐使用 `.mdx`（支持更多功能）

### 2. Front Matter（文档元数据）

每个文档文件开头需要包含 Front Matter：

```mdx
---
title: 文档标题
description: 文档描述（可选）
---

# 文档内容从这里开始
```

### 3. Markdown 语法

支持标准 Markdown 和 GitHub Flavored Markdown (GFM)：

```markdown
# 一级标题（H1）
## 二级标题（H2）
### 三级标题（H3）

**粗体文本**
*斜体文本*

- 无序列表项 1
- 无序列表项 2

1. 有序列表项 1
2. 有序列表项 2

[链接文本](https://example.com)

`行内代码`

\`\`\`javascript
// 代码块
const example = "Hello World"
\`\`\`
```

## 🎨 功能特性

### 自动生成目录（TOC）

文档中的所有标题（H1-H6）会自动：
- 生成唯一 ID（用于锚点链接）
- 显示在右侧 "On this page" 侧边栏
- 支持点击跳转到对应章节

### 左侧导航栏

导航栏配置在 `src/lib/docs/navigation.ts` 中：

```typescript
export const docsNavigation: DocNavItem[] = [
  {
    title: "Get Started",
    href: "/docs",
    children: [
      {
        title: "Welcome to GEO",
        href: "/docs",
      },
      // ...
    ],
  },
  // ...
]
```

### 响应式设计

- 桌面端：左侧导航 + 中间内容 + 右侧目录
- 移动端：自动适配为单列布局

## 🚀 添加新文档

### 步骤 1: 创建文档文件

在 `content/docs/` 目录下创建新的 `.mdx` 文件：

```bash
# 例如：创建产品设置文档
touch content/docs/setup/products.mdx
```

### 步骤 2: 编写文档内容

```mdx
---
title: Product Setup
description: Learn how to set up and manage your products
---

# Product Setup

Your content here...

## Adding a Product

...

## Managing Products

...
```

### 步骤 3: 更新导航配置

编辑 `src/lib/docs/navigation.ts`，添加新文档到导航：

```typescript
{
  title: "Set up Your Project",
  href: "/docs/setup",
  children: [
    // ... 其他文档
    {
      title: "Product Setup",  // 导航显示名称
      href: "/docs/setup/products",  // 文档路径
    },
  ],
}
```

### 步骤 4: 访问文档

文档会自动在以下路径可用：
- `/docs/setup/products` - 直接访问
- 左侧导航栏中显示

## 📝 文档编写建议

1. **使用清晰的标题层级**
   - H1: 文档主标题（通常由 Front Matter 的 title 自动生成）
   - H2: 主要章节
   - H3: 子章节
   - H4-H6: 更细粒度的内容

2. **添加描述性链接**
   ```markdown
   [查看快速开始指南](/docs/quickstart)
   ```

3. **使用代码块时指定语言**
   ```markdown
   ```javascript
   // 代码示例
   ```
   ```

4. **保持文档结构一致**
   - 每个文档都应该有清晰的介绍
   - 使用列表和代码示例增强可读性

## 🔧 自定义样式

文档样式可以通过以下方式自定义：

1. **修改 MDX 组件样式**
   编辑 `src/lib/docs/mdx-renderer.tsx`

2. **修改布局样式**
   编辑 `src/app/docs/layout.tsx`

3. **修改 TOC 样式**
   编辑 `src/components/docs/docs-toc.tsx`

## 📚 示例文档

已创建的示例文档：
- `content/docs/index.mdx` - 欢迎页面
- `content/docs/quickstart.mdx` - 快速开始指南

你可以参考这些文档的格式来编写自己的文档。

## 🎯 下一步

1. 将你的产品说明书 Markdown 文件放入 `content/docs/` 目录
2. 按照上述格式编写或转换文档
3. 更新 `src/lib/docs/navigation.ts` 添加导航项
4. 访问 `/docs` 查看效果

---

**提示**: 文档系统会自动处理文件路径、生成目录、添加锚点链接等功能，你只需要专注于编写内容即可！

